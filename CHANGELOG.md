# Changelog

## 0.3.12 (2026-08-09)

- **新增**：「日常任务登记」命令（`dailyTask`），不依赖 Git 仓库，直接在 VS Code 中创建 DevOps Task 并登记工时
- **改进**：运维工时补录命令同步应用工时类型→任务类型映射
- **新增**：「提交日报」命令（`pushDayWork`），在 VS Code 中一键提交 DevOps 日报，无需打开网页

## 0.3.11 (2026-08-07)

- **新增**：「区域合规检查」命令（`regionCheck`），选择研发项目和产品后，自动拉取本周全部任务，检查任务名称和工时内容中的地名是否与任务所属区域/实施项目匹配
- **检测规则**：规则A — 区域维度检查（"共同区域"不得出现地名，某省份不得出现其他省份地名）；规则B — 实施项目维度检查（任务挂靠的实施项目所在地不得与内容中出现的地名冲突）
- **技术实现**：内置中国行政区划字典（34 省 + 330+ 地级市），纯静态匹配无 AI 依赖；地名匹配按长度优先避免短地名误命中
- **新增依赖**：`fetchTasksByProduct` API（两步查询：分组摘要 → 展开分组），DevOps Task 新增 `regionName`、`opsprojName`、`executeUserName` 等字段

## 0.3.10 (2026-07-23)

- 创建任务时，未完成的任务，支持自定义任务预计完成时间

## 0.3.9 (2026-07-21)

- **完善**：运维工时补录创建 Task 时，支持按 Task 模版结构化填写任务内容，覆盖前置任务、任务描述、开发目录、SVN路径、数据库修改、前端修改、服务端修改、配置修改、验收标准、影响范围共 11 个章节
- **优势**：提供简易/普通/标杆三种模式，简易模式仅需填写任务描述即可快速创建，普通模式补充验收标准，标杆模式逐章节引导完整填写，所有章节均可回车跳过自动使用模版默认值；统一任务内容格式，提升任务规范性和可读性
- **新增配置**：`issueLinkPush.taskCreateMode` — 运维工时补录时 Task 内容填写模式，可选 `simple`（简易，默认）/ `normal`（普通）/ `benchmark`（标杆）

## 0.3.8 (2026-07-20)

- 新增「运维工时补录」命令（`opsWorkHourRecord`），支持一键创建 DevOps 任务、提交推送代码并自动登记工时
- 可缓存仓库和项目/产品记忆，解决频繁选择的问题；可根据事项描述识别devops区域；可同步登记详细工时

## 0.3.7 (2026-06-06)

- 新增手动输入 task/bug 编号查询功能，支持跨用户录入工时（如解决他人身上的task场景）
- 调整获取 task/bug 列表方式，改为从【部门工作项管理】页面加载
- 新增运维模式配置项 `issueLinkPush.debugMode`，开启后在 Issue Link Push 输出通道打印 API 请求/响应调试日志
- 新增版本升级更新通知：VS Code 启动后自动检测版本变化，弹出通知并支持一键查看
- 清除缓存命令同步清除版本记录

## 0.3.5 (2026-05-26)

- 修复工时描述 `•` 重复的bug
- readme文件增加gif描述

## 0.3.4 (2026-05-21)

- 新增「仅提交」命令（`commitOnly`），关联 DevOps 任务后只提交到本地，不推送
- 新增 AI 工时类型自动识别：工时类型名称含 `AI` 时，commit message 中 `-h:` 自动变为 `-aih:`
- 新增工时记录模式配置（追加/覆盖），默认追加
- 新增工时描述记录模式配置（追加/覆盖），默认追加，每条描述以 `•` 开头
- 新增任务百分比记录模式配置（追加/覆盖，上限 100%），默认覆盖
- 新增各输入步骤的模式提示标签
- Git 报错时直接展示 git 原始错误信息

## 0.3.3 (2026-05-21)

- 增加复制按钮


## 0.3.2 (2026-05-21)

- 新增工时类型选择步骤

## 0.3.1 (2026-05-20)
- 新增「关联 DevOps 任务并提交推送」命令（`commitAndPush`），支持新建 commit + 推送的一站式流程1
## 0.3.0 (2026-05-20)

- 兼容本地分支无远程 upstream 的场景，引导用户选择 remote 和分支名后自动 `push --set-upstream`
- 支持多 remote 仓库的分支首次推送选择
- 推送失败时自动撤销 commit amend，保护本地提交数据


## 0.2.0 (2026-05-19)

- 任务列表只加载登陆所属用户的任务
- 获取的任务按照产品线分组

## 0.1.0 (2026-05-18)

- 首次发布
- 支持关联 DevOps 任务/Bug 并自动生成规范提交信息
- 支持推送代码后自动登记工时到 DevOps
- 支持自定义 commit message 模板
- 支持当日工时修改（追加工作内容、覆盖工时）
- 支持清除本地缓存命令
